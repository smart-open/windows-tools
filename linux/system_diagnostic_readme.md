# Java 服务全面系统诊断脚本 使用文档

> **版本**：v1.0.0  
> **日期**：2026-08-05  
> **基于排查体系**：四维定位法（CPU/内存/磁盘/网络）+ CPU 打满四步定位法 + JVM 七种内存错误  
> **适用系统**：CentOS 7+ / Ubuntu 16+ / Debian 9+ / RHEL 7+ / Alpine

---

## 目录

- [一、脚本概述](#一脚本概述)
- [二、快速开始](#二快速开始)
- [三、命令行参数](#三命令行参数)
- [四、交互式菜单](#四交互式菜单)
- [五、诊断模块详解](#五诊断模块详解)
  - [5.1 工具自检与自动安装](#51-工具自检与自动安装)
  - [5.2 系统概览诊断](#52-系统概览诊断)
  - [5.3 CPU 维度诊断](#53-cpu-维度诊断)
  - [5.4 内存维度诊断](#54-内存维度诊断)
  - [5.5 磁盘 I/O 维度诊断](#55-磁盘-io-维度诊断)
  - [5.6 网络维度诊断](#56-网络维度诊断)
  - [5.7 JVM 深度诊断](#57-jvm-深度诊断)
  - [5.8 CPU 四步定位法](#58-cpu-四步定位法)
  - [5.9 综合根因分析](#59-综合根因分析)
- [六、预警阈值参考表](#六预警阈值参考表)
- [七、根因类型与处理建议](#七根因类型与处理建议)
- [八、报告输出说明](#八报告输出说明)
- [九、实战使用示例](#九实战使用示例)
- [十、常见问题](#十常见问题)

---

## 一、脚本概述

`system_diagnostic.sh` 是一个面向 Java 服务的全面系统诊断脚本，整合以下排查体系：

| 排查体系 | 核心方法论 | 脚本对应模块 |
|---------|-----------|------------|
| 系统级四维定位法 | CPU/内存/磁盘/网络逐层排查 | 系统级四维诊断 |
| CPU 高负载四步定位法 | 进程->线程->NID->代码行精准定位 | CPU 四步定位法 |
| JVM 内存与 GC 诊断 | 七种内存错误识别 + GC 调优分析 | JVM 深度诊断 + 根因分析 |

**核心特性**：

- 自动检测并安装缺失的系统诊断工具（vmstat、iostat、sar 等）
- 四维系统资源诊断，集成生产环境验证的预警阈值
- JVM 深度诊断：GC 统计、线程状态分析、死锁检测、堆对象统计
- JVM 安全模式：默认跳过 jmap 等有 STW 风险的操作，使用 jcmd 替代
- CPU 四步定位法全自动执行，直接定位到代码行
- 综合根因分析，自动推断问题类型并给出处理建议
- 交互式菜单 + 命令行参数双模式
- 全程输出彩色终端 + Markdown 报告文件

---

## 二、快速开始

### 2.1 赋予执行权限

```bash
chmod +x system_diagnostic.sh
```

### 2.2 运行脚本

```bash
# 方式一：交互式菜单
./system_diagnostic.sh

# 方式二：一键全面诊断
./system_diagnostic.sh --full

# 方式三：仅系统级诊断
./system_diagnostic.sh --system

# 方式四：仅 JVM 诊断
./system_diagnostic.sh --jvm
```

### 2.3 权限说明

| 操作 | 所需权限 | 说明 |
|------|---------|------|
| 基本诊断 | 普通用户 | top/free/df/vmstat 等只读检查 |
| 工具安装 | root 或 sudo | 安装 sysstat/net-tools 等包 |
| JVM 诊断 | 进程所有者或 root | jstat/jstack/jmap 需与目标进程同用户 |
| dmesg 查看 | root 或 dmesg 权限 | OOM Killer 历史记录检查 |
| 网络连接详情 | root | ss -tnp 查看 PID 级连接信息 |

> **建议**：使用 root 用户运行以获得最完整的诊断信息。非 root 用户运行时，脚本会自动尝试 sudo 安装缺失工具。

---

## 三、命令行参数

| 参数 | 说明 |
|------|------|
| `--full` | 执行全面诊断（系统级 + JVM + 根因分析） |
| `--system` | 仅执行系统级诊断（CPU/内存/磁盘/网络） |
| `--jvm` | 仅执行 JVM 深度诊断 |
| `--allow-jmap` | 启用 jmap 堆分析（有 STW 风险，生产环境慎用） |
| `--no-install` | 禁用工具自动安装（可与其他参数组合使用） |
| `--help` / `-h` | 显示帮助信息 |
| 无参数 | 进入交互式菜单 |

**组合示例**：

```bash
# 全面诊断但不禁用自动安装
./system_diagnostic.sh --full --no-install

# 仅 JVM 诊断且不安装工具
./system_diagnostic.sh --jvm --no-install

# 全面诊断 + 启用jmap堆分析（注意STW风险）
./system_diagnostic.sh --full --allow-jmap
```

---

## 四、交互式菜单

无参数运行脚本时显示以下菜单：

```
╔══════════════════════════════════════════════════════════════╗
║          Java服务全面系统诊断脚本 v1.0.0                      ║
╠══════════════════════════════════════════════════════════════╣
  1) 全面诊断（执行所有检查）
  2) 仅系统级诊断（CPU/内存/磁盘/网络）
  3) 仅JVM诊断
  4) CPU四步定位法（选择Java进程后执行）
  5) 快速概览（仅load average和top）
  6) 工具检查与安装
  0) 退出
```

| 菜单项 | 执行内容 | 适用场景 |
|--------|---------|---------|
| 1 全面诊断 | 工具检查 + 系统概览 + CPU + 内存 + 磁盘 + 网络 + JVM + 根因分析 | 首次全面巡检、故障排查 |
| 2 仅系统级 | 系统概览 + CPU + 内存 + 磁盘 + 网络 + 根因分析 | 排查非 Java 的系统资源问题 |
| 3 仅 JVM | JVM 深度诊断 + 根因分析 | 已确认是 Java 应用层问题 |
| 4 CPU 四步定位法 | 选择 Java 进程 → top -H → printf %x → jstack | CPU 打满紧急排查 |
| 5 快速概览 | load average + top 进程列表 | 快速了解系统状态 |
| 6 工具检查 | 检测并安装缺失工具 | 环境初始化 |

---

## 五、诊断模块详解

### 5.1 工具自检与自动安装

脚本启动时自动检测以下工具的可用性：

**系统工具**：

| 工具 | 所属包 | 用途 |
|------|--------|------|
| vmstat | sysstat | CPU/内存/进程活动统计 |
| iostat | sysstat | 磁盘 I/O 统计 |
| mpstat | sysstat | 多核 CPU 统计 |
| sar | sysstat | 历史性能数据 |
| ss | iproute2 | 套接字统计（替代 netstat） |
| netstat | net-tools | 网络连接统计 |
| lsof | lsof | 文件描述符查看 |
| bc | bc | 浮点数计算 |
| curl | curl | 网络请求 |
| dmesg | 内置 | 内核日志（OOM 记录） |

**JDK 工具**：

| 工具 | 用途 |
|------|------|
| jps | 列出 Java 进程 |
| jstat | GC 统计信息 |
| jstack | 线程堆栈导出 |
| jmap | 堆内存分析 |
| jcmd | JVM 诊断命令 |

**自动安装流程**：

1. 检测系统包管理器（yum → dnf → apt-get → apk）
2. 收集所有缺失工具对应的包名
3. 自动执行安装（非 root 用户自动使用 sudo）
4. 安装后验证工具可用性

> 如果设置了 `JAVA_HOME` 环境变量，脚本会自动将其 `bin` 目录加入 PATH。

### 5.2 系统概览诊断

采集系统基础信息，执行负载趋势分析。

**采集内容**：

- 主机名、操作系统、内核版本、CPU 架构
- CPU 核心数（`nproc`）
- 系统运行时长
- 当前登录用户数

**负载趋势分析逻辑**：

| 负载关系 | 判定 | 含义 |
|---------|------|------|
| 1min < 5min < 15min | 下降 | 系统压力在缓解 |
| 1min > 5min > 15min | 攀升 | 问题正在恶化 |
| 三者接近且都很高 | 持续高压 | 系统处于持续过载状态 |

**负载/核心比阈值检查**：

| 负载/核心比 | 级别 | 处理建议 |
|------------|------|---------|
| > 1.0 | 严重 | 需要立即介入排查 |
| > 0.7 | 警告 | 应引起警惕 |
| <= 0.7 | 正常 | 系统负载健康 |

### 5.3 CPU 维度诊断

基于四维定位法的 CPU 排查体系，回答"谁在吃算力"。

#### 5.3.1 TOP 进程 CPU 概览

执行 `top -bn1`，解析 `%Cpu(s)` 行的六项指标：

| 指标 | 含义 | 警告阈值 | 严重阈值 |
|------|------|---------|---------|
| us | 用户态时间（应用代码消耗） | > 70% | > 90% |
| sy | 内核态时间（系统调用/上下文切换） | > 30% | - |
| id | 空闲时间（越低越忙） | - | - |
| wa | I/O 等待时间 | > 20% | - |
| si | 软中断处理 | - | - |
| st | steal 时间（虚拟机被宿主机偷走） | > 5% | - |

> **关键认知**：wa 高说明瓶颈在磁盘而非 CPU，应转向磁盘维度排查。

#### 5.3.2 vmstat 采样分析

执行 `vmstat 1 5`（每秒采样，共 5 次），解析关键列：

| 列名 | 含义 | 预警阈值 | 排查方向 |
|------|------|---------|---------|
| r | 运行队列进程数 | > CPU 核心数 | CPU 饱和或线程阻塞 |
| b | 不可中断睡眠进程数 | > 0 | I/O 瓶颈 |
| us | 用户态 CPU 时间 | > 70% | 计算密集型任务 |
| sy | 内核态 CPU 时间 | > 30% | 系统调用/上下文切换过多 |
| wa | I/O 等待时间 | > 20% | 磁盘/网络 I/O 瓶颈 |
| cs | 上下文切换次数/秒 | > 100,000 | 线程数过多或锁竞争 |
| si/so | 每秒换入/换出页数 | > 0 | 内存不足，正在交换 |

> **上下文切换风暴识别**：cs > 10万/秒，通常由线程池配置过大、大量线程争抢锁导致。

#### 5.3.3 CPU 占用 TOP 10 进程

执行 `ps aux --sort=-%cpu`，输出 CPU 占用最高的 10 个进程，并单独列出 Java 进程。

### 5.4 内存维度诊断

基于四维定位法的内存排查体系，识别"看不见的战场"。

#### 5.4.1 系统内存概览

执行 `free -h`，重点关注：

| 指标 | 警告阈值 | 严重阈值 | 说明 |
|------|---------|---------|------|
| available / total | < 20% | < 10% | 可用内存占比（非 free 值） |
| Swap used | > 0 | - | 内存压力较大 |

> **关键认知**：Linux 积极利用空闲内存做缓存，`free` 值小是正常的。应关注 `available` 而非 `free`。

#### 5.4.2 内存占用 TOP 10 进程

执行 `ps aux --sort=-%mem`，输出内存占用最高的 10 个进程。

#### 5.4.3 OOM Killer 历史记录

通过 `dmesg` 检查是否发生过 OOM Killer 事件：

- 检测 `out of memory` 和 `killed process` 关键字
- 输出最近 5 条 OOM 记录
- 检测到则标记为严重

#### 5.4.4 大页内存配置

读取 `/proc/meminfo` 中的 HugePages 配置信息。

### 5.5 磁盘 I/O 维度诊断

基于四维定位法的磁盘排查体系，排查"被忽视的瓶颈"。

#### 5.5.1 磁盘空间使用率

执行 `df -h`，检查每个挂载点：

| 使用率 | 级别 | 说明 |
|--------|------|------|
| > 90% | 严重 | 需立即清理或扩容 |
| > 70% | 警告 | 需关注并计划清理 |
| <= 70% | 正常 | - |

#### 5.5.2 磁盘 I/O 统计

执行 `iostat -x 1 3`，解析最后一次采样数据：

| 指标 | 警告阈值 | 严重阈值 | 含义 |
|------|---------|---------|------|
| %util | > 70% | > 90% | 磁盘利用率（接近100%说明饱和） |
| await | > 10ms | - | I/O 响应时间（SSD 正常应 < 1ms） |

#### 5.5.3 D 状态进程检查

检查处于不可中断睡眠（D 状态）的进程，存在则提示可能有 I/O 瓶颈。

### 5.6 网络维度诊断

基于四维定位法的网络排查体系，检查"上下游的连锁反应"。

#### 5.6.1 套接字统计

执行 `ss -s`（或 `netstat -s`），输出套接字汇总信息。

#### 5.6.2 TCP 连接状态分布

统计各状态连接数，重点检查：

| 指标 | 警告阈值 | 含义 |
|------|---------|------|
| TIME-WAIT 数量 | > 5000 | 短连接未被及时回收 |

#### 5.6.3 TCP 重传率

从 `/proc/net/snmp` 读取 TCP 统计，计算重传率：

| 重传率 | 级别 | 含义 |
|--------|------|------|
| > 5% | 严重 | 网络质量差 |
| > 1% | 警告 | 网络质量差或下游拥堵 |
| <= 1% | 正常 | - |

#### 5.6.4 其他网络检查

- 网络接口 IP 地址信息
- 连接队列溢出检查（`/proc/net/netstat`）
- 网络连接数 TOP 进程（需 root 权限）

### 5.7 JVM 深度诊断

基于完整排查体系的 JVM 深度诊断，对每个 Java 进程执行分析。

#### 5.7.1 Java 进程列表

执行 `jps -lv` 列出所有 Java 进程（含主类名和 JVM 参数）。

#### 5.7.2 进程基本信息

| 采集项 | 说明 |
|--------|------|
| 进程名/启动命令 | 进程完整启动参数 |
| CPU 使用率 | 进程级 CPU 占用 |
| 内存使用率 | 进程级内存占用百分比 |
| RSS 内存 | 进程实际物理内存（MB） |
| 启动时间 | 进程启动时间戳 |
| 线程数 | > 1000 触发警告 |

#### 5.7.3 GC 统计分析

执行 `jstat -gcutil <pid> 1000 3`，解析以下指标：

| 列名 | 含义 | 关注要点 |
|------|------|---------|
| S0/S1 | Survivor 区使用率 | 应交替使用，一个始终为 0 可能对象直接晋升 |
| E | Eden 区使用率 | 频繁达 100% 正常，但分配速率过快需关注 |
| O | 老年代使用率 | 持续上升且不下降 = 内存泄漏嫌疑 |
| M | Metaspace 使用率 | 动态类加载过多时可能触发 Full GC |
| YGC/FGC | Young/Full GC 次数 | 结合运行时间计算频率 |
| YGCT/FGCT | Young/Full GC 耗时 | FGC 耗时 > 1s 通常不可接受 |
| GCT | GC 总耗时 | 占总运行时间比例应 < 10% |

**GC 预警检查**：

| 指标 | 警告阈值 | 严重阈值 |
|------|---------|---------|
| 老年代使用率 (O) | > 80% | > 90% |
| Metaspace 使用率 (M) | > 85% | > 95% |
| Full GC 平均耗时 | - | > 1 秒 |
| GC 时间占比 | > 5% | > 10% |

#### 5.7.4 线程状态分析

执行 `jstack <pid>`，统计线程状态分布：

| 状态 | 正常场景 | 异常场景 |
|------|---------|---------|
| RUNNABLE | 正在执行或等待 CPU | 数量过多可能 CPU 饱和 |
| BLOCKED | 等待监视器锁 | > 10 警告，> 50 严重（锁竞争激烈） |
| WAITING | 无限期等待条件 | 需确认等待原因是否合理 |
| TIMED_WAITING | 超时等待 | 大量存在可能是线程池配置不当 |

**附加检查**：

- **死锁检测**：自动检测 jstack 输出中的死锁信息
- **BLOCKED 线程锁等待分析**：提取 `waiting to lock <0x...>` 锁对象信息
- **jstack 文件保存**：完整输出保存到 `/tmp/diag_reports/jstack_<pid>_<timestamp>.txt`

#### 5.7.5 堆内存对象统计（安全模式）

**安全说明**：
- `jmap -histo:live` 会触发 Full GC，导致应用短暂暂停
- `jmap -dump` 会暂停整个 JVM，大堆可能停顿数十秒
- 生产环境建议使用 Arthas heapdump 或配置 `-XX:+HeapDumpOnOutOfMemoryError` 自动 dump

**安全模式（默认）**：
- 跳过 `jmap -histo:live`（会触发 Full GC）
- 优先使用 `jcmd GC.class_histogram`（JDK9+，影响更小）
- 如需完整 jmap 分析，请使用 `--allow-jmap` 参数

**非安全模式**：
- 使用 `jmap -histo`（不含 :live，避免触发 Full GC）

#### 5.7.6 JVM 启动参数分析

解析进程启动参数中的 `-X`、`-XX`、`-D` 选项，检查：

- 堆内存配置（-Xms/-Xmx 是否相等）
- GC 算法选择（UseG1GC/UseZGC 等）
- OOM 自动 dump 配置（HeapDumpOnOutOfMemoryError）
- Metaspace 上限（MaxMetaspaceSize）
- GC 日志配置

### 5.8 CPU 四步定位法

基于四步定位法的核心方法论，自动化执行 CPU 打满的定位流程：

```
top 找进程PID → top -H 找线程TID → printf %x 转NID → jstack 定位代码行
```

**执行步骤**：

| 步骤 | 命令 | 输出 |
|------|------|------|
| 第一步 | `ps -p <PID> -o pcpu=` | 确认目标进程 CPU 使用率 |
| 第二步 | `top -H -bn1 -p <PID>` | 找到 CPU 最高的线程 TID |
| 第三步 | `printf '%x\n' <TID>` | 将十进制 TID 转为十六进制 NID |
| 第四步 | `jstack <PID>` + grep nid | 定位到具体代码行 |

**五大根因参考**（定位到代码行后自动输出）：

1. **死循环/无限递归**：检查循环条件和递归终止条件
2. **频繁 Full GC**：查看 GC 日志，`jstat -gcutil` 确认
3. **正则回溯灾难**：检查正则表达式复杂度，使用预编译 Pattern
4. **锁竞争与伪共享**：检查 synchronized/Lock 使用
5. **计算密集型任务缺乏限流**：添加限流/异步处理/结果缓存

**Arthas 替代命令**（脚本自动提示）：

```bash
thread -n 5              # 查看CPU占用最高的5个线程
thread <tid>             # 查看指定线程堆栈（无需进制转换）
thread --state BLOCKED   # 查看所有BLOCKED线程
thread -b                # 查看阻塞其他线程的线程（持锁者）
```

### 5.9 综合根因分析

脚本根据所有诊断模块的告警标记，自动推断问题根因并给出处理建议。

**支持的根因类型**：

| 根因类型 | 触发条件 | 说明 |
|---------|---------|------|
| I/O 瓶颈 | wa > 20% 或 iostat %util > 70% 或 D 状态进程 | 日志级别误改导致 I/O 饱和等案例 |
| CPU 密集型 | us > 70% | 五大根因：死循环/GC频繁/正则回溯/锁竞争/计算任务 |
| Java 高 CPU | Java 进程 CPU > 50% | 四步定位法快速定位 |
| 内存泄漏/不足 | available < 20% 或 Swap > 0 | 七种内存错误识别 |
| GC 频繁 | FGC > 0 或 GC 时间占比 > 5% | GC 调优建议 |
| 锁竞争/死锁 | BLOCKED > 10 或检测到死锁 | 锁优化建议 |
| 上下文切换风暴 | cs > 100,000/秒 | 3000线程导致 cs 飙升等案例 |
| 网络问题 | TCP 重传 > 1% 或 TIME-WAIT > 5000 | 网络维度排查 |
| 线程数过多 | 线程数 > 1000 | 线程池配置不当 |
| 磁盘空间不足 | df 使用率 > 70% | - |
| 系统过载 | 负载/核心比 > 1.0 | - |
| Metaspace OOM | Metaspace > 95% | 类加载泄漏等 |

每个根因输出：类型、描述、处理建议、详细分析（含排查路径和调优参数）。

---

## 六、预警阈值参考表

以下为脚本内置的所有预警阈值（定义在脚本开头的统一变量区，可按需调整）：

### 6.1 系统负载

| 指标 | 变量名 | 警告 | 严重 |
|------|--------|------|------|
| 负载/核心比 | LOAD_WARN_RATIO / LOAD_CRIT_RATIO | > 0.7 | > 1.0 |

### 6.2 CPU

| 指标 | 变量名 | 警告 | 严重 |
|------|--------|------|------|
| 用户态 CPU (us) | CPU_US_WARN / CPU_US_CRIT | > 70% | > 90% |
| 内核态 CPU (sy) | CPU_SY_WARN | > 30% | - |
| I/O 等待 (wa) | CPU_WA_WARN | > 20% | - |
| 上下文切换 (cs) | CPU_CS_WARN | > 100,000/秒 | - |

### 6.3 内存

| 指标 | 变量名 | 警告 | 严重 |
|------|--------|------|------|
| 可用内存占比 | MEM_AVAIL_WARN / MEM_AVAIL_CRIT | < 20% | < 10% |
| Swap 使用 | - | > 0 | - |

### 6.4 磁盘

| 指标 | 变量名 | 警告 | 严重 |
|------|--------|------|------|
| 磁盘空间使用率 | DISK_SPACE_WARN / DISK_SPACE_CRIT | > 70% | > 90% |
| 磁盘 I/O %util | DISK_UTIL_WARN / DISK_UTIL_CRIT | > 70% | > 90% |
| 磁盘 await (SSD) | DISK_AWAIT_SSD | > 1ms | - |
| 磁盘 await (HDD) | DISK_AWAIT_HDD | > 15ms | - |

### 6.5 网络

| 指标 | 变量名 | 警告 | 严重 |
|------|--------|------|------|
| TIME-WAIT 连接数 | TIME_WAIT_WARN | > 5000 | - |
| TCP 重传率 | TCP_RETRANS_WARN / TCP_RETRANS_CRIT | > 1% | > 5% |

### 6.6 JVM

| 指标 | 变量名 | 警告 | 严重 |
|------|--------|------|------|
| 老年代使用率 (O) | JVM_OLD_GEN_CRIT | > 80% | > 90% |
| Metaspace 使用率 (M) | JVM_METASPACE_CRIT | > 85% | > 95% |
| Full GC 平均耗时 | JVM_FGC_TIME_CRIT | - | > 1000ms |
| GC 时间占比 | JVM_GC_RATIO_CRIT | > 5% | > 10% |
| 线程总数 | JVM_THREAD_WARN | > 1000 | - |
| BLOCKED 线程数 | JVM_BLOCKED_WARN / JVM_BLOCKED_CRIT | > 10 | > 50 |

---

## 七、根因类型与处理建议

### 7.1 I/O 瓶颈

**识别特征**：wa > 20%，iostat %util > 70%，D 状态进程存在

**处理建议**：
- 磁盘性能不足（SSD 应 await < 1ms，HDD < 15ms）
- 随机 I/O 过多（考虑顺序化/批量写）
- 慢 SQL / 大文件读写
- 磁盘硬件故障

**文章案例**：日志级别从 WARN 误改为 DEBUG，大促流量下大量 DEBUG 日志同步写入导致 I/O 饱和。处理：恢复日志级别 + 配置 logback 异步 Appender。

### 7.2 CPU 密集型 / Java 高 CPU

**识别特征**：us > 70%，Java 进程 CPU > 50%

**五大根因排查**：
1. 死循环/无限递归 - 检查循环和递归终止条件
2. 频繁 Full GC - jstat 确认，优化 GC 参数
3. 正则回溯灾难 - 检查正则表达式复杂度
4. 锁竞争与伪共享 - 检查 synchronized 使用
5. 计算密集型任务缺乏限流 - 添加限流/线程池

### 7.3 内存泄漏 / 内存不足

**识别特征**：available < 20%，Swap > 0，OOM Killer 记录存在

**JVM 七种内存错误对照**：

| 错误类型 | 发生区域 | 排查工具 | 关键参数 |
|---------|---------|---------|---------|
| Heap Space | 堆内存 | MAT, jmap | -Xmx, -Xms |
| Metaspace | 元空间 | jcmd VM.metaspace | MaxMetaspaceSize |
| Native Thread | 线程资源 | ps -eLf, ulimit | -Xss, ulimit -u |
| Direct Buffer | 直接内存 | VM.native_memory | MaxDirectMemorySize |
| GC Overhead | GC 效率 | jstat, GC 日志 | UseGCOverheadLimit |
| Array Size | 堆内存 | 代码审查 | 无（int 上限） |
| StackOverflow | 线程栈 | jstack | -Xss |

### 7.4 GC 频繁

**识别特征**：FGC > 0，GC 时间占比 > 5%

**处理建议**：
- 内存泄漏导致老年代快速填满
- 堆大小设置不合理
- 大对象直接进入老年代
- GC 算法选择不当（建议 G1/ZGC）
- 关键参数：`-Xms/-Xmx`, `MaxMetaspaceSize`, `HeapDumpOnOutOfMemoryError`

### 7.5 锁竞争 / 死锁

**识别特征**：BLOCKED > 10，检测到死锁

**处理建议**：
- 锁粒度过大（细化锁范围）
- 锁获取顺序不一致（死锁）
- 伪共享（@Contended / 缓存行填充）
- 使用 synchronized 而非并发容器
- Arthas：`thread -b` 查看阻塞源

### 7.6 上下文切换风暴

**识别特征**：cs > 100,000/秒

**处理建议**：
- 线程数过多（减少线程/使用线程池）
- 锁竞争导致频繁切换
- I/O 阻塞线程过多
- 频繁的系统调用

**文章案例**：某金融系统 3000 线程导致 cs 达 20 万/秒，限制线程池为 CPU 核心数 2 倍+1 后 cs 降至 2 万以下。

---

## 八、报告输出说明

### 8.1 报告文件

| 文件 | 路径 | 说明 |
|------|------|------|
| 诊断报告 | `/tmp/diag_reports/diagnostic_report_YYYYMMDD_HHMMSS.md` | 完整诊断输出（Markdown 格式，已去除颜色码） |
| jstack 输出 | `/tmp/diag_reports/jstack_<pid>_<timestamp>.txt` | 每个 Java 进程的完整线程堆栈 |

### 8.2 报告结构

```
# Java 服务系统诊断报告

> **脚本版本**: 1.0.0
> **生成时间**: 2026-08-05 10:23:45
> **主机名**: server-01
> **操作系统**: Linux 4.18.0 x86_64
> **执行用户**: root

---

## 工具自检与自动安装
...
## 系统概览诊断
...
## CPU维度诊断
...
## 内存维度诊断
...
## 磁盘I/O维度诊断
...
## 网络维度诊断
...
## JVM深度诊断
...
## 综合根因分析
...
## 诊断摘要

| 状态 | 数量 |
|------|------|
| INFO (信息) | 25 |
| OK (正常) | 18 |
| WARN (警告) | 3 |
| CRIT (严重) | 1 |
| FAIL (失败) | 0 |

**报告文件**: `/tmp/diag_reports/diagnostic_report_20260805_102345.md`
```

### 8.3 状态标签说明

| 标签 | 颜色 | 含义 |
|------|------|------|
| [INFO] | 青色 | 信息性输出 |
| [OK] | 绿色 | 检查通过，指标正常 |
| [WARN] | 黄色 | 警告，指标超出警告阈值 |
| [CRIT] | 红色 | 严重，指标超出严重阈值，需立即处理 |
| [FAIL] | 紫色 | 工具不可用或检查执行失败 |

---

## 九、实战使用示例

### 9.1 场景一：凌晨告警，负载飙升

```bash
# 登录服务器后一键全面诊断
./system_diagnostic.sh --full
```

脚本会自动执行四维诊断，如果发现 wa > 20%，会在根因分析中提示 I/O 瓶颈，并建议检查日志级别和磁盘 I/O。

### 9.2 场景二：CPU 打满，响应超时

```bash
# 使用菜单中的 CPU 四步定位法
./system_diagnostic.sh
# 选择 4，然后选择目标 Java 进程
```

脚本自动执行 top -H → printf %x → jstack，直接定位到高 CPU 线程的代码行。

### 9.3 场景三：OOM 告警，频繁 Full GC

```bash
# 仅 JVM 诊断
./system_diagnostic.sh --jvm
```

脚本对每个 Java 进程执行 jstat GC 分析、jstack 线程分析、jmap 对象统计，并在根因分析中提示可能的内存泄漏和 GC 调优建议。

### 9.4 场景四：新服务器环境初始化

```bash
# 仅检查和安装工具
./system_diagnostic.sh
# 选择 6
```

脚本检测所有缺失工具并自动安装。

### 9.5 场景五：CI/CD 集成自动化巡检

```bash
# 非交互模式全面诊断，禁用自动安装
./system_diagnostic.sh --full --no-install

# 检查报告中的 CRIT 数量
grep -c "\[CRIT\]" /tmp/diag_reports/diagnostic_report_*.md
```

---

## 十、常见问题

### Q1: 脚本提示 bash 版本不支持关联数组

脚本使用 Bash 4.0+ 的关联数组（associative array）特性。CentOS 7 默认 Bash 版本为 4.2，支持该特性。如遇旧版系统，建议升级 Bash 或仅使用系统级诊断（根因分析功能可能受限）。

### Q2: jps/jstat/jstack 命令找不到

原因：JDK 未安装或 JAVA_HOME 未设置。

解决方案：
```bash
# 设置 JAVA_HOME
export JAVA_HOME=/path/to/jdk
export PATH=$JAVA_HOME/bin:$PATH

# 或安装 JDK
yum install -y java-1.8.0-openjdk-devel   # CentOS
apt-get install -y openjdk-8-jdk            # Ubuntu
```

### Q3: jstack/jmap 提示权限不足

原因：当前用户与目标 Java 进程的用户不一致。

解决方案：使用 root 用户运行，或使用目标进程的所属用户运行：
```bash
sudo -u <java_process_user> ./system_diagnostic.sh --jvm
```

### Q4: jmap 堆对象统计显示为空/被跳过

原因：脚本默认开启 JVM 安全模式，会跳过有 STW 风险的 jmap 操作。

解决方案：
- 使用 `--allow-jmap` 参数启用 jmap 完整分析（注意生产环境慎用）
- 或者直接使用 `jcmd <pid> GC.class_histogram` 作为替代方案（JDK9+，影响更小）

### Q5: 工具自动安装失败

可能原因：
- 无网络访问权限
- 包管理器源不可用
- 无 sudo 权限

解决方案：手动安装所需包：
```bash
# CentOS/RHEL
yum install -y sysstat net-tools lsof bc curl

# Ubuntu/Debian
apt-get install -y sysstat net-tools lsof bc curl
```

### Q6: 报告文件在哪里

报告文件默认保存在 `/tmp/diag_reports/` 目录下：
```bash
ls -lt /tmp/diag_reports/
```

### Q7: 如何调整预警阈值

所有阈值变量定义在脚本开头的"预警阈值统一定义"区域（第 67-105 行），直接修改对应变量值即可：
```bash
# 例如将 CPU 用户态警告阈值从 70 调整为 60
CPU_US_WARN=60
```

### Q8: 脚本支持容器环境吗

支持。脚本兼容 Docker/Kubernetes 容器环境。在容器内运行时注意：
- 容器内通常没有 systemd，dmesg 可能不可用
- 需要容器内已安装所需工具（或挂载宿主机的工具）
- JVM 诊断需要容器内运行的是 Java 进程

### Q8: 如何仅查看某个 Java 进程的诊断

使用菜单选项 4（CPU 四步定位法）可以选择特定 Java 进程进行诊断。或使用 `--jvm` 模式，脚本会对所有 Java 进程逐一诊断。

---

> **文档版本**：v1.0.0  
> **最后更新**：2026-08-05  
> **脚本文件**：`system_diagnostic.sh`（1618 行，39 个函数）
